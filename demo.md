# qntm Demo Notes

The original Go-era demo walkthrough has been archived at `attic/go/demo.md`.

The active supported surface for this repo is:

- `python-dist/` for the Python CLI/runtime
- `client/` for the TypeScript protocol library
- `ui/` for the user interfaces
- `worker/` and `gateway-worker/` for the active Worker-based services
